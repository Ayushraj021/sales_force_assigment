apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sales-forecasting.fullname" . }}-config
  labels:
    {{- include "sales-forecasting.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
