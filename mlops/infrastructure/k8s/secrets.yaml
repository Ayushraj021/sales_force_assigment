# NOTE: In production, use External Secrets Operator or Sealed Secrets
# This file is a template - do not commit actual secrets!
---
apiVersion: v1
kind: Secret
metadata:
  name: sales-forecasting-secrets
  namespace: sales-forecasting
  labels:
    app.kubernetes.io/name: sales-forecasting
type: Opaque
stringData:
  # Application secrets
  SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"

  # Database credentials
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # Redis credentials
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # S3/MinIO credentials
  S3_ACCESS_KEY: "CHANGE_ME_IN_PRODUCTION"
  S3_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
---
# External Secrets example (if using External Secrets Operator with Vault)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: sales-forecasting-secrets
#   namespace: sales-forecasting
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: sales-forecasting-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: SECRET_KEY
#       remoteRef:
#         key: secret/data/sales-forecasting
#         property: secret_key
#     - secretKey: POSTGRES_PASSWORD
#       remoteRef:
#         key: secret/data/sales-forecasting
#         property: postgres_password
#     - secretKey: REDIS_PASSWORD
#       remoteRef:
#         key: secret/data/sales-forecasting
#         property: redis_password
#     - secretKey: S3_ACCESS_KEY
#       remoteRef:
#         key: secret/data/sales-forecasting
#         property: s3_access_key
#     - secretKey: S3_SECRET_KEY
#       remoteRef:
#         key: secret/data/sales-forecasting
#         property: s3_secret_key
